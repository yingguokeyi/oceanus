
// var name = document.getElementById("uname").value;
// var pas = document.getElementById("pwd").value;
// function fangfa() {
// 	$.ajax({
// 		url: "http://10.1.2.113:8080/uranus/user",
// 		type: "GET",
// 		dataType: "jsonp", //指定服务器返回的数据类型
// 		data: {
// 			method: 'getUser',
// 			loginName: name,
// 			pwd:passw
// 		},
// 		success: function(data) {
// 			console.log("+++++++++++++++++++++++++++++++++++++++++++++++为啥没token")
// 			console.log(data)
// 			var result = JSON.stringify(data); //json对象转成字符串
// 			token = jsel.match('.token', data.result);
// 			fangfaer();
// 		}
// 	});
// }


 			// 	$(".mian_btn_login").click(function() {
				// 	var name = document.getElementById("uname").value;
 			//         var passw = document.getElementById("pwd").value;
 			// 		console.log(name)
			 //        console.log(passw)
 			// 		var pwdreg = /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]{6,12}$/;
				// 	var reg = /^1[3|4|5|7|9|6|8][0-9]\d{4,8}$/;
 			// 		var uname = $('#uname').val().replace(/\s+/g, "");*/
				//     var pwd = $('#pwd').val().replace(/\s+/g, "");
				// 	if(!reg.test(uname)) {
						
				// 		$('.err_uname').text('手机格式不对！');
				// 		layer.open({
				// 		  content: '手机格式不正确',
				// 			  style: 'background-color:#fff; color:#666; border:none;font-size:0.3rem;', //自定风格
				// 			  time: 1
				// 		});
				// 	} else if(!pwdreg.test(pwd)) {
				// 		$('.err_uname').text('密码格式不正确');
				// 	layer.open({
				// 			  content: '密码格式不正确',
				// 			  style: 'background-color:#fff; color:#666; border:none;font-size:0.3rem;', //自定风格
				// 			  time: 1
				// 			});
					
				// 	} else {
						
				// 	}
				// });
				// 	var token = null;
				// 	function fangfa() {
				// 	$.ajax({
				// 		url: "http://10.1.2.113:8080/uranus/user",
				// 		type: "GET",
				// 		dataType: "jsonp", //指定服务器返回的数据类型
				// 		data: {
				// 			method: 'getUser',
				// 			loginName:name,
				// 			pwd:passw
				// 		},
				// 		success: function(data) {
				// 			var result = JSON.stringify(data); //json对象转成字符串
				// 			console.log(data);
				// 			var to=jsel.match('.token',data.result)
				// 			console.log(to)
				// 		}
				// 	});
//                fangfa() ;
					// }
					// function fangfaer() {
					// 		console.log(token)
					// 	}*/
				